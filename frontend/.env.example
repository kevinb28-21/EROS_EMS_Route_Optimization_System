# Frontend configuration — copy to .env for local overrides
#
# Local dev (Vite): leave empty to use the dev-server proxy to http://localhost:8000
# Production (Netlify): set to your Railway backend public URL (no trailing slash)
#   Example: VITE_API_URL=https://your-service.up.railway.app
VITE_API_URL=
